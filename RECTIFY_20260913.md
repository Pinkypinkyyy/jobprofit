# Service Profit website rectify — 13 September 2026

Source: full-site audit dated "checked live 14 Sep 2026", 10 findings.
Repo: Pinkypinkyyy/jobprofit · Live origin: https://www.serviceprofit.com.au/

The live origin is not reachable from this session, so every finding was
checked against the generators that produce the site, not against the
served HTML. Two findings did not survive that check. They are recorded
below with the evidence rather than actioned.

## Red team on the audit

| # | Audit said | What the source shows | Outcome |
|---|---|---|---|
| 1 | `hvac.html`, `electrical.html`, `construction.html` serve "a byte-for-byte copy of the homepage (identical checksum)". Restore the three pages. | Each file is a 463-469 byte redirect stub built by `redirect_home()` in `tools/build_pages.py`. `index.html` is 19.5 KB. The checksums are not identical and never were. The collapse was deliberate: `docs/REDTEAM_ONE_OFFER.md` records the three pages as "fake products", and two tests already lock the behaviour in — `test_old_trade_urls_redirect_home` and `test_sitemap_has_real_pages_not_fake_trades`. Their absence from the sitemap is correct; redirects do not belong in a sitemap. | **Rejected.** Restoring the pages would reverse a decision taken on 12 September and put thin duplicate content back on the site. A 301 is not available: GitHub Pages serves no redirect rules. Canonical to "/" plus meta refresh plus `location.replace` is the strongest consolidation signal this host allows, and it is already in place. Added a viewport tag so the fallback line renders properly on a phone. |
| 4 | The TPB register showing "Professional associations: None on record" means the site and the register "disagree about the one fact the disclaimer depends on". | The TPB professional-association field is self-reported and optional. It is not what makes a Professional Standards scheme apply. Scheme cover turns on IPA membership plus a current Professional Practice Certificate. An empty TPB field is not evidence of non-participation. | **Not actioned in code.** The line stays up. The action is Huong's and unchanged: get written confirmation from IPA that public-practice status is covered by their scheme, and add IPA to the TPB profile. If IPA confirms no cover, the line comes off `footer()` in `tools/shared.py`, which is the single place it is written. |

## Fixed

| # | What changed | Where |
|---|---|---|
| 2 | Review count was 25 in two places and undated. Now `REVIEWS_COUNT` and `REVIEWS_AS_AT` in `tools/build_pages.py`, rendering "30 Google reviews as at September 2026" and "Read all 30 Google reviews". One place to update, and the test reads the same constants so page and test cannot drift. | `tools/build_pages.py`, `tests/test_site.py` |
| 3 | The review block was headed "Google reviews / 5.0 on Google" on a trades site with no statement of whose reviews they are. Added: "Reviews of Pink Accounting, the firm behind Service Profit. 30 reviews as at September 2026. They are not job-costing results." | `tools/build_pages.py` |
| 5 | Hero lost its side gutter on mobile. `.hero-grid` inside the 940px query used a `padding` shorthand, which re-declared the inline padding to 0 and beat `.wrap`'s 18px because it sits later in the file at equal specificity. Both hero padding shorthands are now `padding-block`, so neither can touch the inline axis again. | `styles.css` |
| 5 | Hero primary CTA was `#0E0E12` on a near-black hero photo. Only the nav button had been lifted to white. The hero CTA now matches. | `styles.css` |
| 6 | `rights.html` listed only Pink Accounting and Pink Strategic Accounting. Added Service Profit Accounting, the name this site trades under, registered 24 August 2026. | `tools/build_pages.py` |
| 7 | `callback-cost.mp4` was 20.7 MB. Re-encoded two-pass H.264 at 720x1280, 600k video, 64k mono audio, faststart: **3.4 MB**, same 40.7 second duration, decode verified clean. Player `width`/`height` updated to match. A test now fails if it ever goes back over 5 MB. | `assets/video/callback-cost.mp4`, `tools/build_pages.py`, `tests/test_site.py` |
| 9 | `_captcha` was `false`, leaving a honeypot as the only spam defence on a form carrying revenue band, staff count and free text. Now `true` on every form. The honeypot stays. | `tools/shared.py` |
| 10 | `contact.html` had phone and email only. Added a six-field short form through the same `formsubmit.co` endpoint, with its own `_next` so the confirmation returns to the page the visitor was on rather than bouncing them to `book.html`. | `tools/shared.py`, `tools/build_pages.py` |

`enquiry_form()` also had `_next` hardcoded to `/book.html?sent=1`. It is now a
parameter, so any page carrying a form returns the visitor to that page.

## Not fixed, and why

- **7, captions.** No transcript of the film exists anywhere in the repo and the
  audio was not transcribable in this session. Caption text for a client-facing
  film is not something to invent. Supply the script and the burn-in plus a
  WebVTT track is a small job.
- **8, booking friction and the Friday mismatch.** Microsoft Bookings has
  Fridays closed; `contact.html` says Mon-Fri 9:00-4:30. Both sides of that are
  Bookings admin settings inside the Microsoft product, not repository content.
  Decide which is right and set it there. The site copy is correct if Friday
  trading is correct.
- **9, first-party endpoint.** GitHub Pages serves static files only. Moving off
  `formsubmit.co` needs somewhere to post to. Captcha is on in the meantime.

## Verification

`python3 tools/build_pages.py` is idempotent, and `python3 tests/test_site.py`
passes 26 tests, six of them new:
`test_forms_have_captcha_and_honeypot`,
`test_contact_has_a_form_not_just_phone_and_email`,
`test_hero_keeps_its_side_gutter_on_mobile`,
`test_hero_primary_cta_is_readable_on_the_dark_photo`,
`test_rights_lists_the_trading_business_name`,
`test_callback_video_is_phone_sized`.

Not verified live. The origin is unreachable from this session, so the served
pages need a read-back after deploy.

---

# Whole-site review — same day

The audit above covered ten specific items. This is the broader pass over
message, copy, tone, conversion and SEO that the audit did not do.

## Changed

| Area | Finding | Change |
|---|---|---|
| Conversion | `book.html` asked for **ten required fields including three required essay boxes** before a tradie could reach the calendar. This is the only conversion page on the site and the heaviest ask sits at the top of it. | "What is hurting" stays required, because it is what makes the call useful. "Where is the business now" and "where do you want it in 12 months" are now optional and marked as optional. Eight required fields, one essay box. |
| SEO | Homepage title was 98 characters, so it truncated in results, and it led with the brand rather than the thing buyers search for. | Now "Air con, electrical and construction accountant \| Brisbane" (58). Brand moved to a new `og:site_name`. |
| SEO | `system.html`, `why.html` and `check.html` had 26-28 character titles carrying no service or location term at all. `pricing.html` and `rights.html` ran long. | All six rewritten to fit a result and carry the term someone would actually type. A test now fails any title over 62 characters. |
| SEO | `sameAs` listed Facebook and LinkedIn but **not the Google Business Profile**, even though `RECTIFY_20260911` records it as delivered. It is the strongest entity signal a local firm has, and it was the one missing. | Added to `business_node()`, which `local_business_node()` derives from, so both the homepage and contact schema carry it. |
| Tone | Em dashes in our own copy: the `system.html` lead and four lines of the `rights.html` register list. House style forbids them. | Rewritten as full stops. The one Google review quote keeps its em dash because it is verbatim. A test enforces this. |

## Checked and left alone

- **Internal linking is sound.** Every real page has ten inbound internal links;
  `check.html` is not orphaned, it sits in the footer. The only pages with no
  inbound links are `404.html` and the three redirect stubs, which is correct.
- **No `aggregateRating` schema, and that is right.** The 5.0 and the review
  count come from Google. Marking up third-party reviews as your own
  aggregateRating breaches Google's structured data policy. Leave it off.
- **Claims discipline holds across every page.** Worked examples are labelled as
  worked examples, exclusions are stated next to inclusions, "not unlimited
  work" and "you have not signed anything" appear where the fee does.
- **Logo alt text stays `alt="pink"`.** It looked like an accessibility defect,
  but `docs/PINK_BRAND_ON_SERVICE_PROFIT.md` locks the lowercase pink wordmark
  and the nav logo already sits inside a link carrying
  `aria-label="Pink Accounting, Service Profit"`. Brand lock wins.
- **Fee architecture is coherent.** $550 Compliance, $1,650 Job Profit,
  $2,650 Weekly Visibility, $3,500 Ready to Scale, bookkeeping as an add-on and
  not a plan. Cumulative inclusions are stated. The pricing title deliberately
  does not lead with "from $550", which would anchor buyers on the cheapest
  tier when most files are $1,650.

## Still open, and not code

- **No CI runs on pull requests.** The only workflow deploys Pages on push to
  `main`. `tests/test_site.py` never runs in CI on any branch, so these thirty
  tests only protect the site if someone runs them locally.
- **The homepage carries two competing calls to action** ("Book 15 minutes" and
  "Free 60-second hours check"). Both are reasonable. Which one earns the hero
  is a question for GA4 once there is enough traffic to answer it, not a
  question to settle by opinion now.

---

# Rectification of the review's own findings

The whole-site review above listed concerns without acting on all of them.
This closes them.

| Concern | Change |
|---|---|
| The site sold the weekly look without ever showing it. No artefact anywhere, so a buyer at $1,650 a month had nothing to look at. | New "What you actually get" section on `system.html#weekly`: a styled sample of the Monday output, with the hours variance, the bank split, and the one thing needing a decision. Invented figures, labelled "not a client file". The homepage links to it. |
| Nothing answered the obvious objection: why five times a $300 bookkeeper. | Two new pricing FAQs. One explains it is a different job and says keep your bookkeeper if recording is all you need. The other says plainly that if your jobs land on quote you should be on Compliance at $550 instead. Both render into the FAQPage schema. |
| Enquiry data routed to a third-party US relay, with the endpoint hardcoded across three files. | `FORM_ORIGIN` / `FORM_ENDPOINT` / `FORM_AJAX_ENDPOINT` in `tools/shared.py` now drive both form actions, the ajax path in `nav.js` and the CSP. Moving to a first-party Power Automate endpoint is a three-line change, not a hunt. A test fails if `nav.js` ever carries its own copy again. |
| About 1 MB of unreferenced images deploying. | Removed, and `build_assets.py` no longer rebuilds them. A new test walks every file in `assets/` against every reference in the HTML and CSS and fails on any orphan. That test immediately caught 430 KB of `hvac.*` my own manual grep had missed. |

## Contrast defects found while building the sample

Measured rather than eyeballed, and both were real:

- `--gold` `#B45309` on the docket is **3.84:1**. The docket sits on the dark
  hero and the "miss" figure is the whole point of it. Fails AA.
- `--accent` `#ED1651` on a dark panel is **3.69:1**. Fails AA.

Added `--accent-on-dark` `#FF6E92` (6.03:1) and `--gold-on-dark` `#F0A24B`
(8.38:1), applied to the docket and the sample. A test computes the ratios from
the CSS and fails under 4.5:1, so this cannot drift back.

## Rejected while doing this work

`assets/logo.svg` and `assets/logo-white.svg` were **not the Pink wordmark**.
They were reconstructions setting the word "pink" in Georgia. Wiring them in
would have silently replaced the brand mark with a fake. Both deleted so no
future change picks them up by mistake. The real mark stays as the PNG. If the
genuine vector exists, drop it in and it can be used properly.

## Still outside the repository

- **GA4 key events.** `select_content` fires on every CTA click and
  `generate_lead` on a successful send, so the wiring is there. They still have
  to be marked as key events in the GA4 console before any funnel question can
  be answered. Bookings completes on Microsoft's domain, so a completed booking
  can never be measured here, only the click out.
- **The Power Automate flow itself.** The site side is ready. The flow needs an
  anonymous HTTP trigger inside the Pink tenant, which is a decision about
  exposing an open endpoint, not a website change.

---

# Owner confirmations, 13 September 2026

Answers from Huong closing three items the review left open.

| Item | Answer | What changed |
|---|---|---|
| Are the published fees current? | **Yes.** $550 Compliance, $1,650 Job Profit, $2,650 Weekly Visibility, $3,500 Ready to Scale, bookkeeping from $500, all + GST monthly for one trading entity. | Nothing. Confirmed correct, so the claims review date in the footer moves to 13 September 2026. |
| Is the Professional Standards Legislation line safe? | **Yes.** Huong confirms IPA is correct. | The line stays on every page footer. The 14 September audit's reading of this was wrong for the reason already recorded above: the TPB association field is not what makes a scheme apply. |
| Bookings has Friday closed, the site said Mon-Fri. | **Remove Friday.** | `contact.html` now reads Mon-Thu, 9:00am-4:30pm, and the `openingHoursSpecification` in the schema drops Friday so the structured data matches. A test fails if they diverge again. |

## Still worth doing, not a website change

- The TPB public register shows **Professional associations: None on record**
  against 26284368. IPA cover being correct does not populate that field;
  it is a self-reported entry on the TPB profile and it is still blank. Adding
  it removes the only reason anyone would question the footer line.
- `contact.html` now says closed Friday but **Saturday by appointment**. That
  is what "remove Friday" produces literally. If Saturday should go too, or
  Friday should read "by appointment", say so and it is a one-line change.

## Still open

- `GOOGLE_PLACES_API_KEY` and `GOOGLE_PLACE_ID`, before live Google reviews
  replace the committed 5.0 from 30.
- An anonymised real weekly report to replace the invented figures in the
  `system.html#weekly` sample.
